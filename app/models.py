"""
File: app/models.py

What it does:
  - Defines Python classes that map to database tables (using SQLAlchemy).
  - We’ll have at least one class, e.g. Booking, to store name + canoe count.

Why it’s here:
  - Keeps data definitions all together, so routes just import and use them.
  
"""
